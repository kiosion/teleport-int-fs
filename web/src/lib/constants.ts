export const BACKEND_URL = 'https://localhost:8081';

export const API_URL = `${BACKEND_URL}/api/v1`;

export const JWT_REFRESH_INTERVAL = 1000 * 60 * 30; // 30 minutes

export const JWT_KEY = 'token';

export const LOGIN_REDIRECT_PARAM = 'redirectUri';
